name: Auto Deploy to Ubuntu Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest changes directly to web root
        run: |
          cd /var/www/car-game
          git fetch origin main
          git reset --hard origin/main